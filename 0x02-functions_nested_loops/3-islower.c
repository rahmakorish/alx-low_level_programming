#include "main.h"
/**
 * islower:checks for lowercase char
 * c:character to check
 * Retutn:0succes
 **/
int _islower(int c)
{
	if (c < 90)
	{
		if (c > 65)
	
		return 0;
	}
	
	else
	
		return 1;
	
}
