#include "main.h"
/**
 *_islower:check if input is char
 *c:input
 *Return:0iflowerchar
 *1ifupper
 **/
int _islower(int c)
{
	if (c < 90)
	{
		if (c > 65)
		
			_putchar(1);
	}
	else
		_putchar(0);
}
