#include "main.h"
/**
 * _strlen-returns the length of a astring
 * @s:string input
 * Return:string length
 **/
int _strlen(char *s)
{
	int count = 0;
	int i;

	for (i = 0 ; i < size0f(s); i++)
	
	while (s[i] != '\0')
	{
		count++;
	};
		return(count);
	
}
