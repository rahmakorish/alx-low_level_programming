#include "main.h"
#include <stddef.h>
/**
 * print_list-prints all elements of a list
 * @h:inputted list
 * Return:number of nodes
 **/
size_t print_list(const list_t *h)
{
	if (h == NULL)
	{
		printf("[0] (nil)");
	}
	return(0);
}
