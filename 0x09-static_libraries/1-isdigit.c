#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * *_isdigit-checks for a digit
 * *@c:integer to be checked
 * *Return:0succes
 * **/
int _isdigit(int c)
{
		if (c > 47 && c < 58)
				{
							return (1);
								}
			else
					{
								return (0);
									};
}
